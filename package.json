{
  "name": "clipvault",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node esbuild.config.mjs",
    "icons": "node scripts/make-icons.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "fake-indexeddb": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
